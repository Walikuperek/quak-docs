import VantaHero from "components/ui/sections/vanta-hero";
import {HeaderText} from "../components/ui/generics/header-text";
import {Text} from "../components/ui/generics/text";
import {MainCategoriesBtns} from "../components/ui/buttons/main-categories";
import {QuakV1Hero} from "../components/ui/sections/quak-v1-hero";
import {Spacer} from "../components/ui/generics/spacer";
import {LatestReadings} from "../components/ui/sections/latest-readings";
import {QuakColored} from "../components/ui/quak-colored";
import {BackendPosts} from "../data/backend-posts";
import {FrontendPosts} from "../data/frontend-posts";
import {CloudPosts} from "../data/cloud-posts";
import {AllPosts} from "../data/posts";

<VantaHero />

<HeaderText size="h3" color="gray">LEARN</HeaderText>
<HeaderText size="h2">
  Learn with <QuakColored />
</HeaderText>
<div className="h-2">&nbsp;</div>
<Text color="gray" textLines={[
    "Go for knowledge about programming aspects. QUAK",
    "created multiple apps, games and websites, take this knowledge",
    "from QUAK. Click category to learn more."
]} />
<Spacer height={20}/>

<HeaderText size="h3" color="gray">CATEGORIES</HeaderText>
<HeaderText size="h2">Main categories</HeaderText>
<MainCategoriesBtns />

<section className="bg-lines opacity-30 flex mt-10 mb-20 h-20 w-full"></section>

<LatestReadings />

<section className="bg-lines-accent opacity-10 flex mt-10 mb-20 h-40 w-full"></section>

<HeaderText size="h3" color="gray">LIBRARIES</HeaderText>
<HeaderText size="h2">
  <QuakColored /> Libraries
</HeaderText>
<div className="h-2">&nbsp;</div>
<Text color="gray" textLines={[
    "QUAK during the process of library creation is",
    "focused mostly on end user experience. We devs,",
    "deserved it. Click Library to learn more."
]} />

<section className="lg:w-9/12 mt-10 mx-auto flex flex-col align-center justify-evenly gap-5 w-full">
  <div id="bun_http_card" className="rounded-3xl h-100 bg-orange-400 bg-opacity-10 border-solid border-2 p-3 border-orange-500 rouded-2xl w-full flex flex-col align-center overflow-hidden max-h-96 gap-3">
    <section className="flex flex-col align-center justify-evenly gap-1 px-3 mx-auto">
      <div className="h-12 border-solid border-2 p-2 border-orange-600 rounded-2xl w-full flex flex-col align-center justify-center gap-3">
        <span className="mx-auto font-bold text-lg">Bun-http</span>
      </div>
    </section>

    <span className="text-lg">Very fast HTTP server for Bun. 0 dependencies.</span>

    <div className="flex align-center">
      <a href="/learn/bun/build-http-server" className="px-2 text-center rounded-md whitespace-nowrap bg-orange-500 px-0.75 py-0.5 text-sm font-semibold text-white shadow-sm hover:bg-orange-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        See blog post
      </a>
      <a href="https://github.com/Walikuperek/Bun-http" target="_blank" className="px-2 text-center rounded-md whitespace-nowrap px-0.75 py-0.5 text-sm font-semibold shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        GitHub repo <span aria-hidden="true">→</span>
      </a>
    </div>

    ```tsx
    import { http, Res } from "path/to/lib/http"; // http from bun-http repo
    http.get("/", (req) => {
      return new Res(`Hello from home`);
    });
    http.get("/product/:productId", (req) => {
      return new Res(`Product id: ${req.params.productId}`);
    });
    http.post("/blog/create", (req) => {
      return new Res(`${req.params}, ${req.query}, ${req.body}`);
    });

    const server = http.serve({ port: 3000 });
    console.log(`Listening on port ${server.port}...`);

    /* Example output
    $ bun main.ts
        [0.90ms] ".env"
        Listening on port 3000...
        8:27:48 PM: 200 GET /
        8:27:52 PM: 405 GET /blog/123
        8:27:56 PM: 200 POST /blog/123
        8:27:58 PM: 200 GET /__endpoints // [DEV] view with list of all endpoints
    */
    ```
  </div>

  <div id="qtheme_card" className="rounded-3xl h-100 bg-rose-400 bg-opacity-10 border-solid border-2 p-3 border-rose-500 rouded-2xl w-full flex flex-col align-center overflow-hidden max-h-96 gap-3">
    <section className="flex align-center justify-between gap-1 px-3 w-full mx-auto">
      <img src="/logos/qtheme.png" alt="Qtheme" className="w-14" />
      <div className="h-12 border-solid border-2 p-2 border-rose-600 rounded-2xl flex flex-col align-center justify-center gap-3">
        <span className="mx-auto font-bold text-lg">Qtheme</span>
      </div>
    </section>

    <span className="text-lg">Light/dark/any your app/website. Works with any framework/vanilla.</span>

    <div className="flex align-center">
      <a href="https://quak.com.pl/lib/qtheme/index.html" target="_blank" className="px-2 text-center rounded-md whitespace-nowrap bg-rose-500 px-0.75 py-0.5 text-sm font-semibold text-white shadow-sm hover:bg-rose-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        See website
      </a>
      <a href="https://www.npmjs.com/package/@quak.lib/qtheme" target="_blank" className="px-2 text-center rounded-md whitespace-nowrap px-0.75 py-0.5 text-sm font-semibold shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        NPM package <span aria-hidden="true">→</span>
      </a>
    </div>

    ```tsx
    import { Qtheme } from '@quak.lib/qtheme'
    import { darkTheme } from 'path/to/your/themes'
    Qtheme.setTheme(darkTheme)

    <body className="bg-color text-color">
      <h1 className="text-primary">Hello world!</h1>
      <p>This is regular text color</p>
      <button className="btn">Action</button>
    </body>
    ```
  </div>

  <div id="qstore_card" className="rounded-3xl h-100 bg-green-400 bg-opacity-10 border-solid border-2 p-3 border-green-500 rouded-2xl w-full flex flex-col align-center overflow-hidden max-h-96 gap-3">
    <section className="flex align-center justify-between gap-1 px-3 w-full mx-auto">
      <img src="/logos/qstore.png" alt="Qstore" className="w-14" />
      <div className="h-12 border-solid border-2 p-2 border-green-600 rounded-2xl flex flex-col align-center justify-center gap-3">
        <span className="mx-auto font-bold text-lg">Qstore</span>
      </div>
    </section>

    <span className="text-lg mb-0">Store for any SPA app. Depends on RxJS.<br />Use it as your reactive source of data.</span>
    <span className="text-lg mt-0"></span>

    <div className="flex align-center">
      <a href="/libraries/qstore" className="px-2 text-center rounded-md whitespace-nowrap bg-green-500 px-0.75 py-0.5 text-sm font-semibold text-white shadow-sm hover:bg-green-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        See docs page
      </a>
      <a href="https://github.com/Walikuperek/Qstore" target="_blank" className="px-2 text-center rounded-md whitespace-nowrap px-0.75 py-0.5 text-sm font-semibold shadow-sm focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-white">
        GitHub repo <span aria-hidden="true">→</span>
      </a>
    </div>

    ```tsx
    class CounterStore extends Store<{count: number}> {
      count$ = this.select(state => state.count)

      constructor() { super({count: 0}) }

      add(value: number) {
        this.set({count: this.values.count + value})
      }
    }

    @Component({
      selector: 'app-counter',
      template: `
        <div>
          <span>Count: {{counterStore.count$ | async}}</span>
          <button (click)="counterStore.add(1)">Increment</button>
          <button (click)="counterStore.add(5)">Add 5</button>
        </div>
      `
    })
    class CounterComponent implements OnInit {
      constructor(public counterStore: CounterStore) {}
    }
    ```
  </div>
</section>

<section className="bg-lines-accent opacity-10 flex mt-20 mb-20 h-40 w-full"></section>

<HeaderText size="h3" color="gray">HISTORY</HeaderText>
<HeaderText size="h2"><QuakColored /> History</HeaderText>
<div className="h-2">&nbsp;</div>
<Text color="gray" textLines={[
    "QUAK was always focused on developing full products.",
    "Starting from an idea ending up with working solution.",
    "Everything is created with ❤️"
]} />

<QuakV1Hero />

<Spacer height="10" />
