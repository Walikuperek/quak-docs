import {PostsList} from "../../components/lists/posts-list";
import {NodePosts} from "../../data/tag-posts/node";

# #Node

<h3 className="text-lg mt-10 mb-2 text-gray-500 font-bold">LEARN</h3>
<h2 className="text-4xl mb-3 font-bold">What is Node</h2>
<p className="text-gray-500">Node is a JavaScript runtime built on Chrome's V8 JavaScript engine.</p>
<p className="text-gray-500">Node is known for its asynchronous, non-blocking, single-threaded approach to programming.</p>
<p className="text-gray-500">Node is used for traditional web sites and back-end API services.</p>

<section className="bg-lines flex mt-10 mb-20 h-12 w-full"></section>

<h3 className="text-lg mb-2 text-gray-500 font-bold">READ</h3>
<h2 className="text-4xl mb-3 font-bold">All Node readings</h2>
<p className="mb-10 text-gray-500">Read all Node related articles</p>

<PostsList posts={NodePosts}/>
