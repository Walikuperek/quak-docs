import {Spacer} from "../../components/ui/generics/spacer";
import {TagsList} from "../../components/ui/buttons/tags-list";
import {BunTag} from "../../components/tags/bun";
import {JSTag} from "../../components/tags/javascript";
import {TSTag} from "../../components/tags/typescript";
import {GCPTag} from "../../components/tags/gcp";
import {ArchitectureTag} from "../../components/tags/architecture";
import {AngularTag} from "../../components/tags/angular";
import {ExpressJSTag} from "../../components/tags/expressjs";
import {NestJSTag} from "../../components/tags/nestjs";
import {NextJSTag} from "../../components/tags/nextjs";
import {NodeTag} from "../../components/tags/node";
import {PythonTag} from "../../components/tags/python";
import {ReactTag} from "../../components/tags/react";
import {HeroLearnCard} from "../../components/ui/generics/hero-learn-card";
import {AllPosts, latestReading} from "../../data/posts";
import {PostsList} from "../../components/lists/posts-list";
import {HeaderText} from "../../components/ui/generics/header-text";
import React from "react";
import {Text} from "../../components/ui/generics/text";

# All Readings


<HeroLearnCard
    category={latestReading.color}
    link={latestReading.link}
    tag={latestReading.tag}
    image={latestReading.image}
    author={latestReading.author}
    createdAt={latestReading.createdAt}
    title={latestReading.title}
    descriptionTextLines={latestReading.descriptionTextLines}
    label={<>Latest post</>}
/>

<Spacer />

<article className="bg-neutral-500 bg-opacity-10 mb-20 p-2 pb-5 rounded-3xl">
    <HeaderText size="h3" color="gray">TAGS</HeaderText>
    <HeaderText size="h2">All tags</HeaderText>
    <Text color="gray" textLines={[
        "Tag purpose is to show what is the topic of given",
        "article. Click one to see all related articles."
    ]} />
    <Spacer />

    <hr />
    <Spacer />

    <TagsList>
        <ArchitectureTag />
        <BunTag/>
        <JSTag/>
        <TSTag/>
        <GCPTag/>
        <AngularTag />
        <ExpressJSTag />
        <NestJSTag />
        <NextJSTag />
        <NodeTag />
        <PythonTag />
        <ReactTag />
    </TagsList>
</article>

<h3 className="text-lg mb-2 text-gray-500 font-bold">READ</h3>
<h2 className="text-4xl mb-3 font-bold">All readings</h2>
<p className="mb-10 text-gray-500">You can jump right in and read all articles.</p>

<PostsList posts={AllPosts} />
