import {PostsList} from "../../components/lists/posts-list";
import {GCPPosts} from "../../data/tag-posts/gcp";

# #Google Cloud Platform

<h3 className="text-lg mt-10 mb-2 text-gray-500 font-bold">LEARN</h3>
<h2 className="text-4xl mb-3 font-bold">What is GCP</h2>
<p className="text-gray-500">Google Cloud Platform is a suite of a cloud computing services for your business</p>
<p className="text-gray-500">that runs on the same infrastructure that Google uses internally for its end-user products,</p>
<p className="text-gray-500">such as Google Search, Gmail, file storage, and YouTube.</p>

<section className="bg-lines flex mt-10 mb-20 h-12 w-full"></section>

<h3 className="text-lg mb-2 text-gray-500 font-bold">READ</h3>
<h2 className="text-4xl mb-3 font-bold">All GCP readings</h2>
<p className="mb-10 text-gray-500">Read all GCP related articles</p>

<PostsList posts={GCPPosts}/>
